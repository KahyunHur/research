import { loadQuartzConfig, loadQuartzLayout } from "./quartz/plugins/loader/config-loader"
import * as ExternalPlugin from "./.quartz/plugins"

// ====================================================
// FolderPage ?????? ????????? '?????? ?????? ??????'?????? ????????????
// ====================================================

ExternalPlugin.FolderPage({
  sort: (f1, f2) => {
    // 1. ?????? ????????? ??????(slug)??? ???????????????.
    // (slug??? 'folder/subfolder/note-name' ????????? ?????????.)
    const path1 = f1.slug ?? ""
    const path2 = f2.slug ?? ""
    
    // 2. ?????? ???????????? ???????????? ??????(Z->A) ????????? ???????????????.
    return path2.localeCompare(path1)
  }
})


// ====================================================
// 2. ???????????? "?????? ?????????(Explorer)" ?????? ?????? ??????
// ====================================================
ExternalPlugin.Explorer({
  sortFn: (a, b) => {
    // Explorer??? ?????? ??????(FileNode)??? ??????????????? ?????? ????????? a.file.slug ??? ???????????????.
    // ????????? ?????? file ????????? ?????? ??? ???????????? ?????????(a.name)??? ???????????? ???????????????.
    const pathA = a.file?.slug ?? a.name ?? ""
    const pathB = b.file?.slug ?? b.name ?? ""

    A  // ?????? ?????? ??????(Z->A) ?????? return pathB.localeCompare(pathA)
    }
})

// ====================================================

const config = await loadQuartzConfig()
export default config
export const layout = await loadQuartzLayout()
